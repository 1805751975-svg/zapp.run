import 'package:flutter/material.dart';<br/>import 'package:flutter_svg/flutter_svg.dart';<br/>import 'package:intl/intl.dart';<br/>import 'package:provider/provider.dart';<br/>import 'dart:io';<br/>import 'package:image_picker/image_picker.dart';<br/><br/>void main() {<br/>  runApp(const KangHuTongApp());<br/>}<br/><br/>// 全局数据模型<br/>class AppData extends ChangeNotifier {<br/>  final User _currentUser = User(<br/>    name: "张大爷",<br/>    age: 72,<br/>    healthTags: ["认知关注", "阿尔兹海默症科普"],<br/>    familyMembers: [<br/>      FamilyMember(name: "小张", phone: "13800138000", isBound: true),<br/>    ],<br/>  );<br/><br/>  // 科普前问卷<br/>  final List<Question> preQuestionnaire = [<br/>    Question(id: 1, title: "您了解阿尔兹海默症吗？", options: ["完全不了解", "听说过", "基本了解", "非常了解"]),<br/>    Question(id: 2, title: "您知道大脑细胞损伤与认知的关系吗？", options: ["不知道", "有点了解", "清楚知道"]),<br/>    Question(id: 3, title: "您身边有认知障碍人群吗？", options: ["无", "有家人", "有朋友/邻居"]),<br/>  ];<br/><br/>  // 科普后问卷<br/>  final List<Question> postQuestionnaire = [<br/>    Question(id: 1, title: "本次科普是否帮助您理解阿尔兹海默症？", options: ["没有帮助", "一般", "帮助很大", "非常有帮助"]),<br/>    Question(id: 2, title: "您是否掌握了神经元损伤相关知识？", options: ["未掌握", "基本掌握", "完全掌握"]),<br/>    Question(id: 3, title: "您是否愿意向家人宣传脑健康知识？", options: ["不愿意", "可以考虑", "非常愿意"]),<br/>  ];<br/><br/>  // 阿尔兹海默症 细胞生物学科普内容<br/>  final List<ScienceItem> adScienceList = [<br/>    ScienceItem(<br/>      title: "神经元与突触——大脑的基础单元",<br/>      content: "神经元是神经系统的基本结构，负责传递电信号与化学信号。突触是神经元之间传递信息的关键结构。阿尔兹海默症患者会出现大量突触丢失，直接导致记忆、认知能力下降。",<br/>      type: "text",<br/>    ),<br/>    ScienceItem(<br/>      title: "β-淀粉样蛋白沉积的细胞机制",<br/>      content: "β淀粉样蛋白（Aβ）异常折叠、聚集，形成不溶性斑块，毒害神经元细胞膜，破坏钙离子平衡，导致神经元功能衰竭甚至死亡。",<br/>      type: "text",<br/>    ),<br/>    ScienceItem(<br/>      title: "Tau蛋白过度磷酸化与神经纤维缠结",<br/>      content: "Tau蛋白负责稳定神经元微管。过度磷酸化后，Tau蛋白从微管脱落并聚集，形成神经纤维缠结，阻断营养运输，最终造成神经元死亡。",<br/>      type: "text",<br/>    ),<br/>    ScienceItem(<br/>      title: "脑衰老、炎症与阿尔兹海默症的关系",<br/>      content: "小胶质细胞过度激活引发慢性炎症，进一步加速神经元损伤，形成恶性循环。",<br/>      type: "text",<br/>    ),<br/>  ];<br/><br/>  // 科普资源：彩页 + PPT + 视频<br/>  final List<ResourceItem> resourceList = [<br/>    ResourceItem(title: "阿尔兹海默症科普彩页", type: "彩页", url: "https://example.com/ad_color.pdf"),<br/>    ResourceItem(title: "阿尔兹海默症细胞科普PPT", type: "PPT", url: "https://example.com/ad_cell.ppt"),<br/>    ResourceItem(title: "神经元损伤动画视频", type: "视频", url: "https://example.com/neuron.mp4"),<br/>  ];<br/><br/>  // 科普问答<br/>  final List<QaItem> qaList = [<br/>    QaItem(question: "阿尔兹海默症是正常变老吗？", answer: "不是。正常衰老记忆力下降慢，阿尔兹海默症是神经元大量死亡导致的疾病。"),<br/>    QaItem(question: "β-淀粉样蛋白是什么？", answer: "一种异常蛋白质，沉积后会毒害大脑神经细胞。"),<br/>    QaItem(question: "如何早期保护大脑细胞？", answer: "坚持用脑、运动、睡眠充足、社交活跃、控制三高，可延缓神经元衰老。"),<br/>  ];<br/><br/>  // 现场科普图片（可上传）<br/>  List<File> sceneImages = [];<br/><br/>  User get currentUser => _currentUser;<br/>}<br/><br/>// 数据模型<br/>class User {<br/>  final String name;<br/>  final int age;<br/>  final List<String> healthTags;<br/>  final List<FamilyMember> familyMembers;<br/>  User({required this.name, required this.age, required this.healthTags, required this.familyMembers});<br/>}<br/><br/>class FamilyMember {<br/>  final String name;<br/>  final String phone;<br/>  bool isBound;<br/>  FamilyMember({required this.name, required this.phone, required this.isBound});<br/>}<br/><br/>class Question {<br/>  final int id;<br/>  final String title;<br/>  final List<String> options;<br/>  Question({required this.id, required this.title, required this.options});<br/>}<br/><br/>class ScienceItem {<br/>  final String title;<br/>  final String content;<br/>  final String type;<br/>  ScienceItem({required this.title, required this.content, required this.type});<br/>}<br/><br/>class ResourceItem {<br/>  final String title;<br/>  final String type;<br/>  final String url;<br/>  ResourceItem({required this.title, required this.type, required this.url});<br/>}<br/><br/>class QaItem {<br/>  final String question;<br/>  final String answer;<br/>  QaItem({required this.question, required this.answer});<br/>}<br/><br/>// 主APP<br/>class KangHuTongApp extends StatelessWidget {<br/>  const KangHuTongApp({super.key});<br/>  @override<br/>  Widget build(BuildContext context) {<br/>    return ChangeNotifierProvider(<br/>      create: (context) => AppData(),<br/>      child: MaterialApp(<br/>        title: '康护通',<br/>        theme: ThemeData(<br/>          primarySwatch: Colors.blue,<br/>          textTheme: const TextTheme(<br/>            bodyLarge: TextStyle(fontSize: 22),<br/>            bodyMedium: TextStyle(fontSize: 20),<br/>          ),<br/>        ),<br/>        home: const MainTabPage(),<br/>        debugShowCheckedModeBanner: false,<br/>      ),<br/>    );<br/>  }<br/>}<br/><br/>// 底部导航（删除咨询，保留：首页、科普、问卷、我的）<br/>class MainTabPage extends StatefulWidget {<br/>  const MainTabPage({super.key});<br/>  @override<br/>  State<MainTabPage> createState() => _MainTabPageState();<br/>}<br/><br/>class _MainTabPageState extends State<MainTabPage> {<br/>  int _currentIndex = 0;<br/>  final List<Widget> _tabs = [<br/>    const HomePage(),<br/>    const SciencePage(),<br/>    const QuestionnairePage(),<br/>    const MinePage(),<br/>  ];<br/><br/>  @override<br/>  Widget build(BuildContext context) {<br/>    return Scaffold(<br/>      body: _tabs[_currentIndex],<br/>      bottomNavigationBar: BottomNavigationBar(<br/>        currentIndex: _currentIndex,<br/>        onTap: (i) => setState(() => _currentIndex = i),<br/>        type: BottomNavigationBarType.fixed,<br/>        selectedFontSize: 18,<br/>        items: const [<br/>          BottomNavigationBarItem(icon: Icon(Icons.home), label: '首页'),<br/>          BottomNavigationBarItem(icon: Icon(Icons.school), label: '科普'),<br/>          BottomNavigationBarItem(icon: Icon(Icons.assignment), label: '问卷'),<br/>          BottomNavigationBarItem(icon: Icon(Icons.person), label: '我的'),<br/>        ],<br/>      ),<br/>    );<br/>  }<br/>}<br/><br/>// 首页<br/>class HomePage extends StatelessWidget {<br/>  const HomePage({super.key});<br/>  @override<br/>  Widget build(BuildContext context) {<br/>    final appData = Provider.of<AppData>(context);<br/>    return Scaffold(<br/>      appBar: AppBar(title: const Text('康护通 - 脑健康科普'), centerTitle: true),<br/>      body: SingleChildScrollView(<br/>        padding: const EdgeInsets.all(16),<br/>        child: Column(<br/>          crossAxisAlignment: CrossAxisAlignment.start,<br/>          children: [<br/>            Text("您好，${appData.currentUser.name}！", style: const TextStyle(fontSize: 26, fontWeight: FontWeight.bold)),<br/>            const SizedBox(height: 12),<br/>            const Text("本期主题：阿尔兹海默症与脑细胞保护", style: TextStyle(fontSize: 22, color: Colors.blue)),<br/>            const SizedBox(height: 24),<br/><br/>            // 快速入口<br/>            GridView(<br/>              shrinkWrap: true,<br/>              physics: const NeverScrollableScrollPhysics(),<br/>              gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(crossAxisCount: 2, mainAxisSpacing: 12, crossAxisSpacing: 12),<br/>              children: [<br/>                _item("科普前问卷", Colors.orange, Icons.quiz, () => Navigator.push(context, MaterialPageRoute(builder: (_) => const PreQuestionnairePage()))),<br/>                _item("阿尔兹海默症科普", Colors.blue, Icons.school, () => Navigator.push(context, MaterialPageRoute(builder: (_) => const SciencePage()))),<br/>                _item("科普后问卷", Colors.green, Icons.assignment_turned_in, () => Navigator.push(context, MaterialPageRoute(builder: (_) => const PostQuestionnairePage()))),<br/>                _item("现场照片上传", Colors.purple, Icons.photo_album, () => Navigator.push(context, MaterialPageRoute(builder: (_) => const SceneImagePage()))),<br/>              ],<br/>            ),<br/>          ],<br/>        ),<br/>      ),<br/>    );<br/>  }<br/><br/>  Widget _item(String title, Color color, IconData icon, VoidCallback onTap) {<br/>    return GestureDetector(<br/>      onTap: onTap,<br/>      child: Container(<br/>        decoration: BoxDecoration(color: color, borderRadius: BorderRadius.circular(16)),<br/>        child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [<br/>          Icon(icon, size: 40, color: Colors.white),<br/>          const SizedBox(height: 8),<br/>          Text(title, style: const TextStyle(fontSize: 20, color: Colors.white)),<br/>        ]),<br/>      ),<br/>    );<br/>  }<br/>}<br/><br/>// 科普页面（核心：阿尔兹海默症 + 细胞生物学）<br/>class SciencePage extends StatelessWidget {<br/>  const SciencePage({super.key});<br/>  @override<br/>  Widget build(BuildContext context) {<br/>    final appData = Provider.of<AppData>(context);<br/>    return Scaffold(<br/>      appBar: AppBar(title: const Text('阿尔兹海默症 细胞科普'), centerTitle: true),<br/>      body: SingleChildScrollView(padding: const EdgeInsets.all(16),<br/>        child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [<br/>          const Text("一、科普知识（细胞生物学）", style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),<br/>          const SizedBox(height: 12),<br/>          ...appData.adScienceList.map((e) => Padding(<br/>            padding: const EdgeInsets.only(bottom: 16),<br/>            child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [<br/>              Text(e.title, style: const TextStyle(fontSize: 22, fontWeight: FontWeight.bold, color: Colors.blue)),<br/>              const SizedBox(height: 6),<br/>              Text(e.content, style: const TextStyle(fontSize: 20)),<br/>            ]),<br/>          )),<br/><br/>          const SizedBox(height: 24),<br/>          const Text("二、科普资料下载", style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),<br/>          const SizedBox(height: 12),<br/>          ...appData.resourceList.map((r) => ListTile(<br/>            title: Text(r.title, style: const TextStyle(fontSize: 20)),<br/>            subtitle: Text(r.type),<br/>            leading: const Icon(Icons.file_present),<br/>          )),<br/><br/>          const SizedBox(height: 24),<br/>          const Text("三、科普问答", style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),<br/>          const SizedBox(height: 12),<br/>          ...appData.qaList.map((q) => Padding(<br/>            padding: const EdgeInsets.only(bottom: 12),<br/>            child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [<br/>              Text("Q：${q.question}", style: const TextStyle(fontSize: 20, fontWeight: FontWeight.bold)),<br/>              Text("A：${q.answer}", style: const TextStyle(fontSize: 19, color: Colors.black87)),<br/>              const Divider(),<br/>            ]),<br/>          )),<br/>        ]),<br/>      ),<br/>    );<br/>  }<br/>}<br/><br/>// 问卷总页面<br/>class QuestionnairePage extends StatelessWidget {<br/>  const QuestionnairePage({super.key});<br/>  @override<br/>  Widget build(BuildContext context) {<br/>    return Scaffold(<br/>      appBar: AppBar(title: const Text('科普调查问卷'), centerTitle: true),<br/>      body: Center(<br/>        child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [<br/>          Ele

import 'package:flutter/material.dart';<br/>import 'package:flutter_svg/flutter_svg.dart';<br/>import 'package:intl/intl.dart';<br/>import 'package:provider/provider.dart';<br/>import 'dart:io';<br/>import 'package:image_picker/image_picker.dart';<br/><br/>void main() {<br/>  runApp(const KangHuTongApp());<br/>}<br/><br/>// 全局数据模型<br/>class AppData extends ChangeNotifier {<br/>  final User _currentUser = User(<br/>    name: "张大爷",<br/>    age: 72,<br/>    healthTags: ["认知关注", "阿尔兹海默症科普"],<br/>    familyMembers: [<br/>      FamilyMember(name: "小张", phone: "13800138000", isBound: true),<br/>    ],<br/>  );<br/><br/>  // 科普前问卷<br/>  final List<Question> preQuestionnaire = [<br/>    Question(id: 1, title: "您了解阿尔兹海默症吗？", options: ["完全不了解", "听说过", "基本了解", "非常了解"]),<br/>    Question(id: 2, title: "您知道大脑细胞损伤与认知的关系吗？", options: ["不知道", "有点了解", "清楚知道"]),<br/>    Question(id: 3, title: "您身边有认知障碍人群吗？", options: ["无", "有家人", "有朋友/邻居"]),<br/>  ];<br/><br/>  // 科普后问卷<br/>  final List<Question> postQuestionnaire = [<br/>    Question(id: 1, title: "本次科普是否帮助您理解阿尔兹海默症？", options: ["没有帮助", "一般", "帮助很大", "非常有帮助"]),<br/>    Question(id: 2, title: "您是否掌握了神经元损伤相关知识？", options: ["未掌握", "基本掌握", "完全掌握"]),<br/>    Question(id: 3, title: "您是否愿意向家人宣传脑健康知识？", options: ["不愿意", "可以考虑", "非常愿意"]),<br/>  ];<br/><br/>  // 阿尔兹海默症 细胞生物学科普内容<br/>  final List<ScienceItem> adScienceList = [<br/>    ScienceItem(<br/>      title: "神经元与突触——大脑的基础单元",<br/>      content: "神经元是神经系统的基本结构，负责传递电信号与化学信号。突触是神经元之间传递信息的关键结构。阿尔兹海默症患者会出现大量突触丢失，直接导致记忆、认知能力下降。",<br/>      type: "text",<br/>    ),<br/>    ScienceItem(<br/>      title: "β-淀粉样蛋白沉积的细胞机制",<br/>      content: "β淀粉样蛋白（Aβ）异常折叠、聚集，形成不溶性斑块，毒害神经元细胞膜，破坏钙离子平衡，导致神经元功能衰竭甚至死亡。",<br/>      type: "text",<br/>    ),<br/>    ScienceItem(<br/>      title: "Tau蛋白过度磷酸化与神经纤维缠结",<br/>      content: "Tau蛋白负责稳定神经元微管。过度磷酸化后，Tau蛋白从微管脱落并聚集，形成神经纤维缠结，阻断营养运输，最终造成神经元死亡。",<br/>      type: "text",<br/>    ),<br/>    ScienceItem(<br/>      title: "脑衰老、炎症与阿尔兹海默症的关系",<br/>      content: "小胶质细胞过度激活引发慢性炎症，进一步加速神经元损伤，形成恶性循环。",<br/>      type: "text",<br/>    ),<br/>  ];<br/><br/>  // 科普资源：彩页 + PPT + 视频<br/>  final List<ResourceItem> resourceList = [<br/>    ResourceItem(title: "阿尔兹海默症科普彩页", type: "彩页", url: "https://example.com/ad_color.pdf"),<br/>    ResourceItem(title: "阿尔兹海默症细胞科普PPT", type: "PPT", url: "https://example.com/ad_cell.ppt"),<br/>    ResourceItem(title: "神经元损伤动画视频", type: "视频", url: "https://example.com/neuron.mp4"),<br/>  ];<br/><br/>  // 科普问答<br/>  final List<QaItem> qaList = [<br/>    QaItem(question: "阿尔兹海默症是正常变老吗？", answer: "不是。正常衰老记忆力下降慢，阿尔兹海默症是神经元大量死亡导致的疾病。"),<br/>    QaItem(question: "β-淀粉样蛋白是什么？", answer: "一种异常蛋白质，沉积后会毒害大脑神经细胞。"),<br/>    QaItem(question: "如何早期保护大脑细胞？", answer: "坚持用脑、运动、睡眠充足、社交活跃、控制三高，可延缓神经元衰老。"),<br/>  ];<br/><br/>  // 现场科普图片（可上传）<br/>  List<File> sceneImages = [];<br/><br/>  User get currentUser => _currentUser;<br/>}<br/><br/>// 数据模型<br/>class User {<br/>  final String name;<br/>  final int age;<br/>  final List<String> healthTags;<br/>  final List<FamilyMember> familyMembers;<br/>  User({required this.name, required this.age, required this.healthTags, required this.familyMembers});<br/>}<br/><br/>class FamilyMember {<br/>  final String name;<br/>  final String phone;<br/>  bool isBound;<br/>  FamilyMember({required this.name, required this.phone, required this.isBound});<br/>}<br/><br/>class Question {<br/>  final int id;<br/>  final String title;<br/>  final List<String> options;<br/>  Question({required this.id, required this.title, required this.options});<br/>}<br/><br/>class ScienceItem {<br/>  final String title;<br/>  final String content;<br/>  final String type;<br/>  ScienceItem({required this.title, required this.content, required this.type});<br/>}<br/><br/>class ResourceItem {<br/>  final String title;<br/>  final String type;<br/>  final String url;<br/>  ResourceItem({required this.title, required this.type, required this.url});<br/>}<br/><br/>class QaItem {<br/>  final String question;<br/>  final String answer;<br/>  QaItem({required this.question, required this.answer});<br/>}<br/><br/>// 主APP<br/>class KangHuTongApp extends StatelessWidget {<br/>  const KangHuTongApp({super.key});<br/>  @override<br/>  Widget build(BuildContext context) {<br/>    return ChangeNotifierProvider(<br/>      create: (context) => AppData(),<br/>      child: MaterialApp(<br/>        title: '康护通',<br/>        theme: ThemeData(<br/>          primarySwatch: Colors.blue,<br/>          textTheme: const TextTheme(<br/>            bodyLarge: TextStyle(fontSize: 22),<br/>            bodyMedium: TextStyle(fontSize: 20),<br/>          ),<br/>        ),<br/>        home: const MainTabPage(),<br/>        debugShowCheckedModeBanner: false,<br/>      ),<br/>    );<br/>  }<br/>}<br/><br/>// 底部导航（删除咨询，保留：首页、科普、问卷、我的）<br/>class MainTabPage extends StatefulWidget {<br/>  const MainTabPage({super.key});<br/>  @override<br/>  State<MainTabPage> createState() => _MainTabPageState();<br/>}<br/><br/>class _MainTabPageState extends State<MainTabPage> {<br/>  int _currentIndex = 0;<br/>  final List<Widget> _tabs = [<br/>    const HomePage(),<br/>    const SciencePage(),<br/>    const QuestionnairePage(),<br/>    const MinePage(),<br/>  ];<br/><br/>  @override<br/>  Widget build(BuildContext context) {<br/>    return Scaffold(<br/>      body: _tabs[_currentIndex],<br/>      bottomNavigationBar: BottomNavigationBar(<br/>        currentIndex: _currentIndex,<br/>        onTap: (i) => setState(() => _currentIndex = i),<br/>        type: BottomNavigationBarType.fixed,<br/>        selectedFontSize: 18,<br/>        items: const [<br/>          BottomNavigationBarItem(icon: Icon(Icons.home), label: '首页'),<br/>          BottomNavigationBarItem(icon: Icon(Icons.school), label: '科普'),<br/>          BottomNavigationBarItem(icon: Icon(Icons.assignment), label: '问卷'),<br/>          BottomNavigationBarItem(icon: Icon(Icons.person), label: '我的'),<br/>        ],<br/>      ),<br/>    );<br/>  }<br/>}<br/><br/>// 首页<br/>class HomePage extends StatelessWidget {<br/>  const HomePage({super.key});<br/>  @override<br/>  Widget build(BuildContext context) {<br/>    final appData = Provider.of<AppData>(context);<br/>    return Scaffold(<br/>      appBar: AppBar(title: const Text('康护通 - 脑健康科普'), centerTitle: true),<br/>      body: SingleChildScrollView(<br/>        padding: const EdgeInsets.all(16),<br/>        child: Column(<br/>          crossAxisAlignment: CrossAxisAlignment.start,<br/>          children: [<br/>            Text("您好，${appData.currentUser.name}！", style: const TextStyle(fontSize: 26, fontWeight: FontWeight.bold)),<br/>            const SizedBox(height: 12),<br/>            const Text("本期主题：阿尔兹海默症与脑细胞保护", style: TextStyle(fontSize: 22, color: Colors.blue)),<br/>            const SizedBox(height: 24),<br/><br/>            // 快速入口<br/>            GridView(<br/>              shrinkWrap: true,<br/>              physics: const NeverScrollableScrollPhysics(),<br/>              gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(crossAxisCount: 2, mainAxisSpacing: 12, crossAxisSpacing: 12),<br/>              children: [<br/>                _item("科普前问卷", Colors.orange, Icons.quiz, () => Navigator.push(context, MaterialPageRoute(builder: (_) => const PreQuestionnairePage()))),<br/>                _item("阿尔兹海默症科普", Colors.blue, Icons.school, () => Navigator.push(context, MaterialPageRoute(builder: (_) => const SciencePage()))),<br/>                _item("科普后问卷", Colors.green, Icons.assignment_turned_in, () => Navigator.push(context, MaterialPageRoute(builder: (_) => const PostQuestionnairePage()))),<br/>                _item("现场照片上传", Colors.purple, Icons.photo_album, () => Navigator.push(context, MaterialPageRoute(builder: (_) => const SceneImagePage()))),<br/>              ],<br/>            ),<br/>          ],<br/>        ),<br/>      ),<br/>    );<br/>  }<br/><br/>  Widget _item(String title, Color color, IconData icon, VoidCallback onTap) {<br/>    return GestureDetector(<br/>      onTap: onTap,<br/>      child: Container(<br/>        decoration: BoxDecoration(color: color, borderRadius: BorderRadius.circular(16)),<br/>        child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [<br/>          Icon(icon, size: 40, color: Colors.white),<br/>          const SizedBox(height: 8),<br/>          Text(title, style: const TextStyle(fontSize: 20, color: Colors.white)),<br/>        ]),<br/>      ),<br/>    );<br/>  }<br/>}<br/><br/>// 科普页面（核心：阿尔兹海默症 + 细胞生物学）<br/>class SciencePage extends StatelessWidget {<br/>  const SciencePage({super.key});<br/>  @override<br/>  Widget build(BuildContext context) {<br/>    final appData = Provider.of<AppData>(context);<br/>    return Scaffold(<br/>      appBar: AppBar(title: const Text('阿尔兹海默症 细胞科普'), centerTitle: true),<br/>      body: SingleChildScrollView(padding: const EdgeInsets.all(16),<br/>        child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [<br/>          const Text("一、科普知识（细胞生物学）", style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),<br/>          const SizedBox(height: 12),<br/>          ...appData.adScienceList.map((e) => Padding(<br/>            padding: const EdgeInsets.only(bottom: 16),<br/>            child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [<br/>              Text(e.title, style: const TextStyle(fontSize: 22, fontWeight: FontWeight.bold, color: Colors.blue)),<br/>              const SizedBox(height: 6),<br/>              Text(e.content, style: const TextStyle(fontSize: 20)),<br/>            ]),<br/>          )),<br/><br/>          const SizedBox(height: 24),<br/>          const Text("二、科普资料下载", style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),<br/>          const SizedBox(height: 12),<br/>          ...appData.resourceList.map((r) => ListTile(<br/>            title: Text(r.title, style: const TextStyle(fontSize: 20)),<br/>            subtitle: Text(r.type),<br/>            leading: const Icon(Icons.file_present),<br/>          )),<br/><br/>          const SizedBox(height: 24),<br/>          const Text("三、科普问答", style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),<br/>          const SizedBox(height: 12),<br/>          ...appData.qaList.map((q) => Padding(<br/>            padding: const EdgeInsets.only(bottom: 12),<br/>            child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [<br/>              Text("Q：${q.question}", style: const TextStyle(fontSize: 20, fontWeight: FontWeight.bold)),<br/>              Text("A：${q.answer}", style: const TextStyle(fontSize: 19, color: Colors.black87)),<br/>              const Divider(),<br/>            ]),<br/>          )),<br/>        ]),<br/>      ),<br/>    );<br/>  }<br/>}<br/><br/>// 问卷总页面<br/>class QuestionnairePage extends StatelessWidget {<br/>  const QuestionnairePage({super.key});<br/>  @override<br/>  Widget build(BuildContext context) {<br/>    return Scaffold(<br/>      appBar: AppBar(title: const Text('科普调查问卷'), centerTitle: true),<br/>      body: Center(<br/>        child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [<br/>          ElevatedButton(<br/>            style: ElevatedButton.styleFrom(padding: const EdgeInsets.symmetric(horizontal: 32, vertical: 16), fontSize: 22),<br/>            onPressed: () => Navigator.push(context, MaterialPageRoute(builder: (_) => const PreQuestionnairePage())),<br/>            child: const Text("<span class="emoji emoji1f4d1"></span> 科普前调查问卷"),<br/>          ),<br/>          const SizedBox(height: 20),<br/>          ElevatedButton(<br/>            style: ElevatedButton.styleFrom(padding: const EdgeInsets.symmetric(horizontal: 32, vertical: 16), fontSize: 22),<br/>            onPressed: () => Navigator.push(context, MaterialPageRoute(builder: (_) => const PostQuestionnairePage())),<br/>            child: const Text("✅ 科普后调查问卷"),<br/>          ),<br/>        ]),<br/>      ),<br/>    );<br/>  }<br/>}<br/><br/>// 科普前问卷<br/>class PreQuestionnairePage extends StatelessWidget {<br/>  const PreQuestionnairePage({super.key});<br/>  @override<br/>  Widget build(BuildContext context) {<br/>    final appData = Provider.of<AppData>(context);<br/>    return Scaffold(appBar: AppBar(title: const Text("科普前问卷")), body: ListView(padding: const EdgeInsets.all(16), children: [...appData.preQuestionnaire.map((q) => _questionItem(q))]));<br/>  }<br/>  Widget _questionItem(Question q) {<br/>    return Padding(padding: const EdgeInsets.only(bottom: 20),<br/>      child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [<br/>        Text(q.title, style: const TextStyle(fontSize: 22, fontWeight: FontWeight.bold)),<br/>        const SizedBox(height: 8),<br/>        ...q.options.map((o) => RadioListTile(value: o, groupValue: null, onChanged: (v){}, title: Text(o, style: const TextStyle(fontSize: 20)))).toList(),<br/>      ]),<br/>    );<br/>  }<br/>}<br/><br/>// 科普后问卷<br/>class PostQuestionnairePage extends StatelessWidget {<br/>  const PostQuestionnairePage({super.key});<br/>  @override<br/>  Widget build(BuildContext context) {<br/>    final appData = Provider.of<AppData>(context);<br/>    return Scaffold(appBar: AppBar(title: const Text("科普后问卷")), body: ListView(padding: const EdgeInsets.all(16), children: [...appData.postQuestionnaire.map((q) => _questionItem(q))]));<br/>  }<br/>  Widget _questionItem(Question q) {<br/>    return Padding(padding: const EdgeInsets.only(bottom: 20),<br/>      child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [<br/>        Text(q.title, style: const TextStyle(fontSize: 22, fontWeight: FontWeight.bold)),<br/>        const SizedBox(height: 8),<br/>        ...q.options.map((o) => RadioListTile(value: o, groupValue: null, onChanged: (v){}, title: Text(o, style: const TextStyle(fontSize: 20)))).toList(),<br/>      ]),<br/>    );<br/>  }<br/>}<br/><br/>// 现场科普图片上传<br/>class SceneImagePage extends StatefulWidget {<br/>  const SceneImagePage({super.key});<br/>  @override<br/>  State<SceneImagePage> createState() => _SceneImagePageState();<br/>}<br/><br/>class _SceneImagePageState extends State<SceneImagePage> {<br/>  final ImagePicker picker = ImagePicker();<br/><br/>  Future<void> _uploadImage() async {<br/>    final pickedFile = await picker.pickImage(source: ImageSource.gallery);<br/>    if (pickedFile != null) {<br/>      setState(() {<br/>        Provider.of<AppData>(context, listen: false).sceneImages.add(File(pickedFile.path));<br/>      });<br/>    }<br/>  }<br/><br/>  @override<br/>  Widget build(BuildContext context) {<br/>    final images = Provider.of<AppData>(context).sceneImages;<br/>    return Scaffold(<br/>      appBar: AppBar(title: const Text("现场科普照片上传"), centerTitle: true),<br/>      body: Column(children: [<br/>        const SizedBox(height: 20),<br/>        ElevatedButton.icon(onPressed: _uploadImage, icon: const Icon(Icons.add_photo_alternate), label: const Text("选择并上传照片", style: TextStyle(fontSize: 20))),<br/>        const SizedBox(height: 20),<br/>        Expanded(<br/>          child: GridView.builder(gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(crossAxisCount: 3, mainAxisSpacing: 8, crossAxisSpacing: 8),<br/>            itemCount: images.length, itemBuilder: (context, index) {<br/>              return Image.file(images[index], fit: BoxFit.cover);<br/>            }),<br/>        ),<br/>      ]),<br/>    );<br/>  }<br/>}<br/><br/>// 我的页面<br/>class MinePage extends StatelessWidget {<br/>  const MinePage({super.key});<br/>  @override<br/>  Widget build(BuildContext context) {<br/>    return Scaffold(appBar: AppBar(title: const Text("我的"), centerTitle: true), body: const Center(child: Text("个人中心", style: TextStyle(fontSize: 24))));<br/>  }<br/>}
未选择文件
